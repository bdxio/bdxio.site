export class AppNavMenuComponent implements ng.IDirective {
    public template: string = `
        <nav class="navbar navbar-inverse navbar-fixed-top">
            <span style="color: white">Here we have a navbar menu</span>
        </nav>
    `;
}