module.exports = {
  /**
   * Simple example.
   * Every monday at 1am.
   */
  // "* * * * * *": ({ strapi }) => {
  //   console.log("this is a new cron", strapi);
  //   // Add your own logic here (e.g. send a queue of email, create a database backup, etc.).
  // },
};
